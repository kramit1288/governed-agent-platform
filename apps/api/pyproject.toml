[project]
name = "governed-agent-api"
version = "0.1.0"
description = "FastAPI application for the governed agent platform."
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.116,<1.0",
  "uvicorn[standard]>=0.34,<1.0",
]
