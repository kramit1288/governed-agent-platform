# Scripts

Utility scripts for development, maintenance, and repeatable repository tasks belong here.

`seed_data.py` ingests `data/seed/docs.json` into the retrieval chunk store using deterministic embeddings.
