[project]
name = "governed-agent-gateway"
version = "0.1.0"
description = "Model gateway and routing package."
requires-python = ">=3.12"
dependencies = [
  "httpx>=0.28,<1.0",
  "pydantic>=2.11,<3.0",
  "pytest>=8.3,<9.0",
]
